> import pytest
  
> from src.masks import get_mask_account, get_mask_card_number
  
  
  # Параметризация теста маскирования номера счета
> @pytest.mark.parametrize(
>     "input_data, expected_output",
>     [
>         ("73654108430135874305", "**4305"),
>         ("1234567890", "**7890"),  # Короткий номер счета
>         ("", ""),  # Пустой номер счета
>     ],
> )
> def test_get_mask_account(input_data, expected_output):
  
      # Тестируем функцию маскирования номера счета
>     result = get_mask_account(input_data)
>     assert result == expected_output
  
  
  # Параметризация теста маскирования номера карты
> @pytest.mark.parametrize(
>     "input_data, expected_output",
>     [
>         ("7000792289606361", "7000 79** **** 6361"),
>         ("1234567890", ""),  # Короткий номер счета карты
>         ("", ""),  # Пустой номер счета карты
>     ],
> )
> def test_get_mask_card_number(input_data, expected_output):
  
      # Тестируем функцию маскирования счета
>     result = get_mask_card_number(input_data)
>     assert result == expected_output
